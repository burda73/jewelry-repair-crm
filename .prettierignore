node_modules
dist
build
.next
out
coverage
*.tsbuildinfo
infra/docker/data
infra/backup/out
*.md
package-lock.json
# Локальные артефакты и отладочные скрипты (каталог в .gitignore):
# prettier в CI их не увидит, но локальная проверка иначе «падает» на них.
.local/
