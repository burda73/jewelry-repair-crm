# CRM управления и контроля операций ремонта ювелирных изделий

Система для ювелирного сервисного центра: единая база заказов, контроль сроков на каждом
этапе, фиксация согласований с клиентом (включая записи звонков), контроль предоплат и оплат,
отчётность по загрузке, срокам и выручке.

> **Текущий статус: сквозной сценарий приёма заказа работает.** Ответы заказчика на
> блокирующие вопросы получены (`docs/00-decisions.md` §1.1). Реализация ведётся
> по плану из `docs/09-roadmap.md`. Доменное ядро (статусы, права, деньги,
> сроки, номера и QR-коды) реализовано и покрыто **97 тестами**. Система развёрнута
> в LXC и доступна по HTTPS (`https://repair.remixgold.ru/`,
> `infra/db/deployment-report.md`).
>
> Реализовано и проверено фактическим запуском: вход и сессии, ролевой доступ с
> изоляцией магазинов, список и карточка заказа, перевод статусов по матрице
> переходов, глобальный поиск, дашборд, справочники (магазины, цеха, категории,
> камни, прейскурант), клиенты с нормализацией телефона, платежи с идемпотентностью
> и сторно, а также **мастер создания заказа из 5 шагов** (клиент → согласие →
> изделие → работы → итог) с расчётом суммы доменными функциями.
>
> Отложено по решению заказчика: интеграция с 1С и IP-АТС (`docs/05-integrations.md`).
> В работе: печать квитанции с QR-кодом, логистика и партии, отчётность, гарантия
> и рекламации.

---

## Ключевые параметры внедрения

| Параметр | Значение |
|----------|----------|
| Размещение | On-premise, **Proxmox VE 7.4** |
| Домен и TLS | **remixgold.ru**, Let's Encrypt — через **существующий nginx-proxy** заказчика |
| Развёртывание | Docker Compose в LXC (вложенный Docker разрешён) — [`docs/14-deployment.md`](docs/14-deployment.md) |
| База данных | **Существующий PostgreSQL 16 предприятия** — [`infra/db/README.md`](infra/db/README.md) |
| Маршрутизация | Внешний прокси; проект своего прокси не содержит ([`infra/proxy/README.md`](infra/proxy/README.md)) |
| Роли | 8 (`RECEIVER`, `PRODUCTION_MANAGER`, `LOGISTICIAN`, `CASHIER`, `MANAGER`, `ADMIN`, `AUDITOR`, `CHIEF_ACCOUNTANT`) |
| Интеграция с 1С | Фиксация оплаты созданием `ЧекККМ` с услугой «Ремонт» |
| Поиск заказа | QR-код на квитанции, сканер USB (эмуляция клавиатуры) |
| Печать | PDF, офисный принтер A4, из браузера |
| Прейскурант | **24 позиции** утверждённого документа, **цена зависит от металла изделия** (золото/серебро); утверждение руководителем **или** главным бухгалтером |
| Нормативы сроков | Стартовые значения, корректируются заказчиком без разработчика |

---

## Быстрый старт

### Требования

| Компонент | Версия | Обязателен |
|-----------|--------|-----------|
| Node.js | ≥ 20 (проверено на 26) | да |
| npm | ≥ 10 | да |
| Docker + Docker Compose | любая актуальная | **нет** — есть способ без Docker |
| PostgreSQL | 16 | **в продакшне — существующий сервер предприятия** |
| Redis | 7 | пока не используется в коде |

### Важно про базу данных

**В продакшне PostgreSQL — существующий сервер предприятия (ответ A1):** проект его
**не устанавливает** и не поднимает в Docker, а использует уже настроенный PostgreSQL 16.
БД общая с 1С и другими системами, поэтому подготовка базы, права роли, расширения и
бюджет соединений согласуются с администратором БД:

```bash
# Подготовка БД (выполняет администратор): infra/db/README.md §1
# Проверка готовности перед развёртыванием:
export DATABASE_URL="postgresql://repair_app:<пароль>@pg-server.remixgold.ru:5432/repair"
./infra/db/preflight.sh
```

**Локально для разработки** есть два пути: PostgreSQL из npm-пакета без Docker
(`npm run local:setup` — рекомендуется) или контейнер (`npm run infra:up`). В обоих случаях
разработчику не нужен доступ к продовой базе.

### Установка

Есть два способа поднять локальную среду. Выбирайте по наличию Docker.

#### Способ А. Без Docker (проще, ничего не устанавливается в систему)

```bash
npm install
npm run local:setup   # поднимает PostgreSQL 16.14, создаёт .env, применяет схему и seed
npm run dev           # API + веб
```

`local:setup` поднимает **настоящий PostgreSQL 16.14** из npm-пакета
(`scripts/local-db.mjs`): бинарники лежат в `node_modules`, данные — в `.local/postgres`,
права root не нужны. Версия совпадает с продакшном, поэтому поведение БД и сортировка
русских ФИО такие же, как на сервере.

```bash
npm run local:db:status   # состояние локальной БД
npm run local:db:stop     # остановить
npm run local:db:reset    # удалить данные и создать заново
```

#### Способ Б. Через Docker (полный набор сервисов)

```bash
npm install
cp .env.example .env      # замените JWT_*_SECRET и CSRF_SECRET
npm run infra:up          # PostgreSQL, Redis, MinIO, Mailhog
npm run db:migrate
npm run db:seed
npm run dev
```

MinIO: http://localhost:9001 (minioadmin/minioadmin) · Mailhog: http://localhost:8025

> **Про `npm run db:generate`.** Отдельной командой его выполнять не нужно:
> `npm install` вызывает `scripts/postinstall.mjs`, который генерирует Prisma
> Client автоматически. Шаг нужен только после установки с `--omit=dev`, когда
> Prisma CLI не ставится, — скрипт в этом случае предупредит и пропустит
> генерацию, не роняя установку.

#### Адреса после запуска

| Сервис | Адрес |
|--------|-------|
| Веб-интерфейс | http://localhost:3000 |
| API | http://localhost:4000/api/v1 |
| Swagger | http://localhost:4000/api/docs |
| Проверка живости | http://localhost:4000/health/live |
| Готовность (проверяет БД) | http://localhost:4000/health/ready |

**Вход после seed:** `receiver1@remixgold.ru` или `admin@remixgold.ru`, пароль
`DemoPassword123`. Полный список из 9 учётных записей печатает сам seed.

> **Этот пароль известен всем** — он лежит в открытом репозитории. Он допустим
> только в локальной разработке. `seed.ts` отказывается работать при
> `NODE_ENV=production` (обойти можно лишь явным `SEED_ALLOW_PRODUCTION=yes`),
> а для смены уже созданных паролей есть
> `node scripts/rotate-demo-passwords.mjs` — см. «Переключение в продакшн».

> **Redis и S3 в текущей реализации не используются** — модули интеграций ещё не
> написаны. Для запуска достаточно PostgreSQL. Переменные `REDIS_URL`, `S3_*`
> обязательны в конфигурации, но соединение не устанавливается.

### Проверка, что всё работает

```bash
npm run typecheck   # проверка типов
npm run lint        # линт
npm test            # тесты (доменное ядро)
```

---

## Переключение в продакшн

Проект разрабатывается и проверяется на предпродакшн-базе: в ней накапливаются
тестовые заказы, клиенты, платежи и записи аудита. Перед началом реальной
эксплуатации их нужно удалить — иначе в отчёты и журнал попадут данные проверок.

Очистка выполняется скриптом `scripts/purge-test-data.mjs`. По умолчанию он
**только показывает план и ничего не меняет**:

```bash
DATABASE_URL="postgresql://repair_app:…@10.10.0.119:5432/repair" \
  node scripts/purge-test-data.mjs
```

Удаление — по явному флагу `--apply`:

```bash
# 1. Удалить транзакционные данные (заказы, клиенты, платежи, аудит, сессии)
DATABASE_URL="…" node scripts/purge-test-data.mjs --apply

# 2. Дополнительно удалить файлы фотографий с диска
DATABASE_URL="…" STORAGE_LOCAL_DIR=/opt/repair/data/files \
  node scripts/purge-test-data.mjs --apply --files

# 3. Дополнительно удалить демонстрационные учётные записи.
#    ОБЯЗАТЕЛЬНО назвать ту учётную запись администратора, которая останется.
DATABASE_URL="…" node scripts/purge-test-data.mjs --apply --users \
  --keep-admin=director@remixgold.ru
```

**Что сохраняется:** сеть магазинов и цехов, прейскурант с утверждённой версией,
виды камней, категории работ, рабочий календарь, шаблоны уведомлений,
исполнители, нормы этапов, настройки. Это настроечные данные, их в ходе работы
не создают.

**Что удаляется:** заказы, изделия, работы, камни, платежи, согласования,
корректировки, история статусов, фотографии и файлы, партии и акты, рекламации,
уведомления, обмен с внешними системами, записи разговоров, отказы, клиенты,
журнал аудита, сессии входа, счётчики номеров заказов.

**Нумерация заказов** после очистки начинается заново с `…-000001`: счётчик
создаётся при первом заказе.

### Ротация демонстрационных паролей

Пароль `DemoPassword123` из `seed.ts` опубликован в репозитории вместе с
адресами девяти учётных записей, и `gitleaks` его не находит: для сканера это
не секрет. Если такие учётные записи уже созданы и используются, их пароли
нужно сменить — скриптом `scripts/rotate-demo-passwords.mjs`. Он тоже
**по умолчанию только показывает план**:

```bash
DATABASE_URL="…" node scripts/rotate-demo-passwords.mjs
```

Применение генерирует каждой учётной записи **отдельный** пароль из 20 символов
(Argon2id с теми же параметрами, что у API), ставит `mustChangePassword` — при
первом входе система попросит задать свой пароль — и **отзывает все активные
сессии**: без этого refresh-токен жил бы ещё 30 дней, и ротация не закрыла бы
доступ тому, кто уже вошёл старым паролем.

```bash
# Показать пароли в терминале и сохранить в файл (права 600, каталог .local/)
DATABASE_URL="…" node scripts/rotate-demo-passwords.mjs --apply \
  --out=.local/prod-passwords.txt

# Только часть учётных записей
DATABASE_URL="…" node scripts/rotate-demo-passwords.mjs --apply \
  --emails=admin@remixgold.ru,manager@remixgold.ru
```

Пароли показываются **один раз**: в базе хранится только хеш, восстановить их
нельзя. Скрипт отказывается работать, если у учётной записи нет ни одной роли:
пароль такой записи войти всё равно не позволит, и «ротация» создала бы ложное
чувство защищённости. Если ротация нужна только части учётных записей, а
остальные без ролей — используйте `--emails`.

### Пароли, которые нельзя оставлять

`seed.ts` создаёт демонстрационные учётные записи с общим паролем. Запуск
`npm run db:seed` на боевой базе **запрещён**: при `NODE_ENV=production` скрипт
завершается ошибкой. Обойти запрет можно только осознанно —
`SEED_ALLOW_PRODUCTION=yes` — и тогда он печатает предупреждение.

**Почему демо-аккаунты удаляются только по явному адресу.** Все девять учётных
записей из `seed.ts` демонстрационные, и у администратора тот же общеизвестный
пароль `DemoPassword123`. Удаление «всех, кроме администраторов» сохранило бы
учётную запись с известным паролем, а вариант «удалить всех» оставил бы систему
без входа. Поэтому скрипт требует назвать конкретную учётную запись, которая
останется: перед шагом 3 создайте реального администратора и смените пароли.

Скрипт останавливается, если в базе появилась таблица, не отнесённая ни к
тестовым, ни к настроечным: молча пропустить такие данные нельзя.

## Структура проекта

```
├── apps/
│   ├── api/          NestJS — REST API, доменные сервисы, воркеры
│   └── web/          Next.js — PWA для сотрудников + публичные страницы
├── packages/
│   ├── db/           Prisma schema, миграции, seed
│   └── shared/       Доменное ядро: статусы, права, деньги, даты, Zod-схемы
├── docs/             Проектная документация — начните отсюда
├── infra/
│   ├── docker/       docker-compose для разработки и продакшна
│   └── backup/       Скрипты резервного копирования и учебного восстановления
└── .github/workflows CI: линт, типы, тесты, миграции, сборка, безопасность
```

**Ключевой принцип:** `packages/shared` — единый источник правды по домену.
Статусная модель, матрица прав и схемы валидации используются и бэкендом, и фронтендом,
поэтому правила не могут разойтись между ними.

---

## Документация

Читать в этом порядке:

| Документ | О чём |
|----------|-------|
| [`docs/00-decisions.md`](docs/00-decisions.md) | **Ответы заказчика и принятые решения.** Главный документ при расхождениях с ТЗ |
| [`docs/01-architecture.md`](docs/01-architecture.md) | Архитектура, слои, технические решения, бэкапы, развёртывание |
| [`docs/02-domain-and-roles.md`](docs/02-domain-and-roles.md) | Роли, права, области видимости, бизнес-правила |
| [`docs/03-data-model.md`](docs/03-data-model.md) | Модель данных: сущности, инварианты, индексы, миграции |
| [`docs/04-status-workflow.md`](docs/04-status-workflow.md) | **Статусная модель:** 14 статусов, 22 перехода, нормативы, эскалации |
| [`docs/05-integrations.md`](docs/05-integrations.md) | 1С (outbox, идемпотентность), IP-АТС (сопоставление звонков), S3 |
| [`docs/06-reporting.md`](docs/06-reporting.md) | 5 отчётов ТЗ, метрики, кэш, сверка с 1С |
| [`docs/07-api-spec.md`](docs/07-api-spec.md) | REST API, коды ошибок, идемпотентность, пагинация |
| [`docs/08-ui-ux.md`](docs/08-ui-ux.md) | Экраны, карточка заказа, мобильные сценарии, дизайн-система |
| [`docs/09-roadmap.md`](docs/09-roadmap.md) | **План разработки:** 6 этапов, 20 недель, оценки, риски |
| [`docs/10-nfr-security.md`](docs/10-nfr-security.md) | Производительность, безопасность, 152-ФЗ, бэкапы, эксплуатация |
| [`docs/11-open-questions.md`](docs/11-open-questions.md) | **Открытые вопросы к заказчику** (раздел A блокирует старт) |
| [`docs/12-requirements-traceability.md`](docs/12-requirements-traceability.md) | **Трассировка ТЗ → код и тесты** — основание для приёмки |
| [`docs/13-glossary.md`](docs/13-glossary.md) | Глоссарий: единая терминология бизнеса и кода |
| [`docs/14-deployment.md`](docs/14-deployment.md) | **Развёртывание**: Proxmox, Docker в LXC, внешний nginx-proxy (UI), существующий PostgreSQL |
| [`docs/15-known-issues.md`](docs/15-known-issues.md) | **Запуск и известные особенности**: дефекты, найденные при первом старте, ограничения |
| [`infra/db/README.md`](infra/db/README.md) | **Требования к внешнему PostgreSQL 16**: база, роль, расширения, бюджет соединений, бэкапы |
| [`docs/adr/`](docs/adr/README.md) | Architecture Decision Records: почему решения приняты именно так |

---

## Ключевые проектные решения

1. **Мастер-ювелир — не пользователь системы.** По решению заказчика исполнитель производства
   учитывается как справочная запись (`Performer`), а все взаимодействия с производством ведёт
   менеджер обработки поступающих ремонтов. Подробнее: `docs/00-decisions.md` §2.
2. **Цена фиксируется в момент приёма.** Прейскурант версионируется, в заказ копируется цена
   и ссылка на версию. Пересмотр цен не меняет принятые заказы (ТЗ п. 2.2).
   **Цена зависит от металла сданного изделия:** утверждённый прейскурант задаёт отдельные
   цены по золоту и серебру, а сервер проверяет присланную цену и отклоняет расхождение
   (`409 PRICE_MISMATCH`). Подробнее: ADR 0009, `docs/03` §3.3.1.
3. **Деньги — только целые числа** в копейках. `Float` для денег запрещён.
4. **Статус — конечный автомат** с явной таблицей переходов и guard-условиями.
   Валидация только на сервере, UI получает список доступных действий от API.
5. **1С не блокирует приём оплаты.** Платежи пишутся в БД и в transactional outbox;
   синхронизация выполняется воркером с ретраями. 1С может быть выключена — магазин работает.
6. **Разграничение по магазинам:** приёмщик видит свои заказы + глобальный поиск по точному
   номеру (нужно для приёма оплаты в чужой точке). Ответ на вопрос 4 ТЗ.
7. **PWA вместо нативного приложения.** Решение заказчика; один код, работа с телефона,
   фото с камеры, установка на домашний экран.

---

## Разработка

### Полезные команды

```bash
npm run dev:api        # только API
npm run dev:web        # только веб
npm run db:studio      # Prisma Studio — просмотр данных
npm run db:reset       # пересоздать БД и залить seed
npm run infra:logs     # логи инфраструктуры
npm test               # тесты
npm run typecheck      # типы
```

### Соглашения

- **Коммиты:** Conventional Commits (`feat:`, `fix:`, `docs:`, `refactor:`, `test:`).
- **Ветки:** `main` (продакшн) ← `develop` ← `feature/<краткое-имя>`.
- **Бизнес-значения не хардкодятся** — нормативы, шаблоны, форматы номеров и лимиты
  живут в БД или переменных окружения.
- **Изменение статусной модели** требует одновременного обновления
  `docs/04-status-workflow.md` и теста `docs-sync` — иначе CI упадёт.
- **Прямая загрузка заказа по `id` без scope-фильтра запрещена** — защита от IDOR.

### Перед коммитом

```bash
npm run lint && npm run typecheck && npm test
```

---

## Что дальше

1. **Ответить на вопросы раздела A** в `docs/11-open-questions.md` — они блокируют старт.
2. Согласовать прототипы интерфейса (этап 0 плана).
3. Обследовать 1С и IP-АТС — это самые рискованные интеграции (риски R1–R3).

План с оценками и сроками: [`docs/09-roadmap.md`](docs/09-roadmap.md).