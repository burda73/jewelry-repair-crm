-- Адрес заказчика для печатной формы квитанции (требование заказчика).
--
-- Хранится у КЛИЕНТА, а не у заказа: адрес принадлежит человеку, а не
-- отдельному обращению. Столбец nullable — у существующих клиентов адреса нет,
-- и заставлять заполнять его задним числом нечем.
ALTER TABLE "customer" ADD COLUMN "address" TEXT;
