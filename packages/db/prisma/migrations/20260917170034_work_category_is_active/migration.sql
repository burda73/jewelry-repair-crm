-- Задача 1.3.1 (CRUD справочников): у категорий работ появился признак
-- «выведена из обращения».
--
-- Миграция ТОЛЬКО ДОБАВЛЯЕТ столбец с NOT NULL DEFAULT true, поэтому все
-- существующие категории остаются активными, данные не переписываются, и
-- откат не требует восстановления из архива. Столбец добавлен вместо удаления
-- категорий: на `work_category` ссылаются позиции прейскуранта, а на них —
-- созданные заказы, поэтому удаление категории разорвало бы историю расчётов
-- (docs/03-data-model.md §1, правило о версионируемых справочниках).

-- AlterTable
ALTER TABLE "work_category" ADD COLUMN     "isActive" BOOLEAN NOT NULL DEFAULT true;
